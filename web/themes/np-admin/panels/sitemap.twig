{% extends '@np-admin/panel_2col.twig' %}

{% block panel_class %}np_sitemap_panel{% endblock %}

{% block panel_nav %}Sitemap{% endblock %}

{% block column_1 %}
    {# map node types to fa icons #}
    {% set node_icons = {
        'homepage': 'home',
        'page'    : 'file',
        'section' : 'folder',
        'blog'    : 'comment',
        'gallery' : 'picture-o'
        }
    %}
    
    <ul class="list-unstyled np_sm">
        {{ include('@np-admin/partials/_sitemapItems.twig', {'nodes': sitemap.children}) }}
    </ul>
    
    <script id="np_node_types_temp" type="text/html">
        <ul class="nav nav-list" data-node-id="<%= nodeId %>">
        {% for type in node_types %}
            <li><a href="#" class="-np_sm_add_type" data-node-type="{{ type.name }}">{{ font_icon(node_icons[type.name|lower], 'fa-fw') }}{{ type.name }}</a></li>
        {% endfor %}
        </ul>
    </script>

{% endblock %}

{% block column_2 %}
    <div id="np_sm_node_config" class="np_hide">{# ajax loaded here #}</div>
{% endblock %}

{#
    <h3>Extensions</h3>
    <ul>
    {% for extension in extensions %}
        <li><a href="#">{{ extension.name }}</a></li>
    {% endfor %}
    </ul>
#}